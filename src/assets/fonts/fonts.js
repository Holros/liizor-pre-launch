import { Lato, Raleway, Urbanist } from "next/font/google";

export const urbanist = Urbanist({subsets: ["latin"]})
export const lato = Lato({subsets:["latin"], weight:["400","700"]})
export const raleway = Raleway({subsets:["latin"]})
